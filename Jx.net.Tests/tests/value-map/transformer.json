{
  "FullName": "{{$.firstName}} {{$.lastName}}",
  "IsAlive": "{{$.isAlive => BoolYesNo}}", // example of pipe-transform
  "Age": "{{$.age => ToInt}}",
  "primaryContact": "{{$.phoneNumbers[?(@.type == 'mobile')]}}",
  "primaryContactActive": "{{$.phoneNumbers[?(@.type == 'mobile')].isActive => ToBool}}"
  // example of json branch copy
}